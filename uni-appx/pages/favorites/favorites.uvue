<template>
	<view class="page">
		<view class="status-bar"></view>

		<view class="nav-bar">
			<view class="back-icon" @click="goBack"></view>
			<text class="nav-title">收藏</text>
			<view class="nav-right"></view>
		</view>

		<scroll-view class="content" scroll-y>
			<text class="section-title">全部收藏</text>

			<view class="fav-list">
				<view
					class="fav-item"
					v-for="item in favList"
					:key="item.id"
					@click="onItemClick(item)"
				>
					<view class="icon-box" :class="`icon-${item.type}`">
						<view class="type-icon">
							<view class="i1"></view>
							<view class="i2"></view>
						</view>
					</view>
					<view class="fav-info">
						<text class="fav-title">{{ item.title }}</text>
						<text class="fav-preview">{{ item.preview }}</text>
						<text class="fav-date">{{ item.date }}</text>
					</view>
					<view class="arrow"></view>
				</view>
			</view>
		</scroll-view>
	</view>
</template>

<script setup lang="uts">
	import { ref } from 'vue'

	type FavItem = {
		id: string
		type: string
		title: string
		preview: string
		date: string
	}

	const favList = ref<FavItem[]>([
		{
			id: '1',
			type: 'note',
			title: '项目会议纪要',
			preview: '讨论了方案评审与下周排期安排···',
			date: '2026-08-18'
		},
		{
			id: '2',
			type: 'image',
			title: '设计稿截图',
			preview: '首页与详情页共 3 张',
			date: '2026-08-17'
		},
		{
			id: '3',
			type: 'link',
			title: '产品官网长文',
			preview: '来自 36氪 · 行业深度分析',
			date: '2026-08-15'
		},
		{
			id: '4',
			type: 'location',
			title: '公司地址',
			preview: '成都 · 天府广场',
			date: '2026-08-12'
		}
	])

	const goBack = () => {
		uni.navigateBack()
	}

	const onItemClick = (item: FavItem) => {
		uni.navigateTo({ url: '/pages/favorite-detail/favorite-detail' })
	}
</script>

<style>
	.page {
		display: flex;
		flex-direction: column;
		height: 100%;
		background-color: #F7F7F7;
	}

	.status-bar {
		height: var(--status-bar-height);
		background-color: #F8F8F8;
	}

	.nav-bar {
		display: flex;
		flex-direction: row;
		align-items: center;
		justify-content: space-between;
		height: 44px;
		padding: 0 8px;
		background-color: #F8F8F8;
		border-bottom: 1px solid #E5E5E5;
	}

	.back-icon {
		width: 11px;
		height: 11px;
		border-left: 2px solid #333;
		border-bottom: 2px solid #333;
		transform: rotate(45deg);
		margin-left: 8px;
	}

	.nav-title {
		font-size: 17px;
		color: #000;
		font-weight: 600;
	}

	.nav-right {
		width: 32px;
	}

	.content {
		flex: 1;
		background-color: #F7F7F7;
	}

	.section-title {
		padding: 12px 16px;
		font-size: 13px;
		color: #999;
	}

	.fav-list {
		background-color: #FFFFFF;
	}

	.fav-item {
		display: flex;
		flex-direction: row;
		align-items: center;
		padding: 12px 16px;
		background-color: #FFFFFF;
		border-bottom: 1px solid #E5E5E5;
	}

	.icon-box {
		width: 50px;
		height: 50px;
		border-radius: 8px;
		display: flex;
		justify-content: center;
		align-items: center;
		margin-right: 12px;
		position: relative;
	}

	.icon-note {
		background-color: #07C160;
	}

	.icon-note .type-icon {
		width: 16px;
		height: 20px;
		border: 2px solid #fff;
		border-radius: 2px;
		position: relative;
	}

	.icon-note .type-icon .i1 {
		position: absolute;
		top: 4px;
		left: 50%;
		width: 8px;
		height: 2px;
		background-color: #fff;
		transform: translateX(-50%);
	}

	.icon-note .type-icon .i2 {
		position: absolute;
		top: 9px;
		left: 50%;
		width: 8px;
		height: 2px;
		background-color: #fff;
		transform: translateX(-50%);
	}

	.icon-image {
		background-color: #B2B2B2;
	}

	.icon-image .type-icon {
		width: 20px;
		height: 16px;
		border: 2px solid #fff;
		border-radius: 2px;
		position: relative;
	}

	.icon-image .type-icon .i1 {
		position: absolute;
		top: 3px;
		left: 3px;
		width: 5px;
		height: 5px;
		background-color: #fff;
		border-radius: 50%;
	}

	.icon-image .type-icon .i2 {
		position: absolute;
		bottom: 2px;
		left: 2px;
		width: 0;
		height: 0;
		border-left: 8px solid transparent;
		border-right: 8px solid transparent;
		border-bottom: 7px solid #fff;
	}

	.icon-link {
		background-color: #007AFF;
	}

	.icon-link .type-icon {
		width: 14px;
		height: 8px;
		border: 2px solid #fff;
		border-radius: 4px;
		position: relative;
		transform: rotate(-45deg);
	}

	.icon-link .type-icon .i1 {
		position: absolute;
		top: -5px;
		left: 50%;
		width: 2px;
		height: 5px;
		background-color: #fff;
		transform: translateX(-50%);
	}

	.icon-link .type-icon .i2 {
		position: absolute;
		bottom: -5px;
		left: 50%;
		width: 2px;
		height: 5px;
		background-color: #fff;
		transform: translateX(-50%);
	}

	.icon-location {
		background-color: #FF3B30;
	}

	.icon-location .type-icon {
		width: 14px;
		height: 14px;
		border: 2px solid #fff;
		border-radius: 50% 50% 50% 0;
		transform: rotate(-45deg);
		position: relative;
	}

	.icon-location .type-icon .i1 {
		position: absolute;
		top: 50%;
		left: 50%;
		width: 6px;
		height: 6px;
		background-color: #fff;
		border-radius: 50%;
		transform: translate(-50%, -50%);
	}

	.fav-info {
		flex: 1;
		display: flex;
		flex-direction: column;
		justify-content: center;
		overflow: hidden;
	}

	.fav-title {
		font-size: 16px;
		color: #333;
		font-weight: 500;
	}

	.fav-preview {
		font-size: 14px;
		color: #999;
		margin-top: 4px;
		lines: 1;
		text-overflow: ellipsis;
		overflow: hidden;
	}

	.fav-date {
		font-size: 12px;
		color: #B2B2B2;
		margin-top: 4px;
	}

	.arrow {
		width: 8px;
		height: 8px;
		border-top: 2px solid #C8C8C8;
		border-right: 2px solid #C8C8C8;
		transform: rotate(45deg);
		margin-left: 8px;
	}
</style>

<template>
	<view class="page">
		<!-- 自定义导航栏 -->
		<view class="nav-bar">
			<view class="back-arrow" @click="goBack"></view>
			<text class="nav-title">联系人</text>
		</view>

		<scroll-view class="scroll" scroll-y>
			<!-- 资料卡片 -->
			<view class="profile-card">
				<view class="big-avatar">
					<text class="big-avatar-text">林</text>
				</view>
				<text class="nickname">林晚</text>
				<view class="profile-row">
					<text class="profile-label">微信号：</text>
					<text class="profile-value">linwan_88</text>
				</view>
				<view class="profile-row">
					<text class="profile-label">地区：</text>
					<text class="profile-value">四川成都</text>
				</view>
			</view>

			<!-- 菜单 -->
			<view class="menu-group">
				<view class="menu-item arrow" @click="onMenu('设置备注和标签')">
					<text class="menu-label">设置备注和标签</text>
					<view class="right-arrow"></view>
				</view>
				<view class="menu-item arrow" @click="onMenu('朋友权限')">
					<text class="menu-label">朋友权限</text>
					<view class="right-arrow"></view>
				</view>
				<view class="menu-item arrow menu-item-last" @click="onMenu('朋友圈')">
					<text class="menu-label">朋友圈</text>
					<text class="menu-extra">2 条</text>
					<view class="right-arrow"></view>
				</view>
			</view>

			<!-- 底部按钮 -->
			<view class="bottom-actions">
				<view class="btn btn-primary" @click="onMessage">发消息</view>
				<view class="btn btn-outline btn-last" @click="onCall">音视频通话</view>
			</view>
		</scroll-view>
	</view>
</template>

<script setup lang="uts">
	const goBack = () => {
		uni.navigateBack({})
	}

	const onMenu = (title: string) => {
		uni.showToast({ title, icon: 'none' })
	}

	const onMessage = () => {
		uni.navigateTo({ url: '/pages/chat/chat' })
	}

	const onCall = () => {
		uni.showToast({ title: '音视频通话', icon: 'none' })
	}
</script>

<style scoped>
	.page {
		flex: 1;
		background-color: #EDEDED;
	}

	.nav-bar {
		height: 44px;
		background-color: #fff;
		display: flex;
		flex-direction: row;
		align-items: center;
		justify-content: center;
		position: relative;
		border-bottom: 1px solid #E5E5E5;
	}

	.back-arrow {
		position: absolute;
		left: 12px;
		top: 50%;
		transform: translateY(-50%) rotate(45deg);
		width: 11px;
		height: 11px;
		border-left: 2px solid #333;
		border-bottom: 2px solid #333;
	}

	.nav-title {
		font-size: 17px;
		color: #000;
		font-weight: 500;
	}

	.scroll {
		flex: 1;
	}

	.profile-card {
		background-color: #fff;
		padding: 24px 16px;
		display: flex;
		flex-direction: column;
		align-items: center;
		margin-bottom: 10px;
	}

	.big-avatar {
		width: 72px;
		height: 72px;
		border-radius: 12px;
		background-color: #F5A623;
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.big-avatar-text {
		font-size: 32px;
		color: #fff;
		font-weight: 500;
	}

	.nickname {
		font-size: 20px;
		color: #000;
		font-weight: 500;
		margin-top: 12px;
	}

	.profile-row {
		display: flex;
		flex-direction: row;
		margin-top: 6px;
	}

	.profile-label {
		font-size: 14px;
		color: #999;
	}

	.profile-value {
		font-size: 14px;
		color: #999;
	}

	.menu-group {
		background-color: #fff;
		margin-bottom: 10px;
	}

	.menu-item {
		display: flex;
		flex-direction: row;
		align-items: center;
		padding: 0 16px;
		height: 50px;
		border-bottom: 1px solid #F0F0F0;
	}

	.menu-item-last {
		border-bottom: none;
	}

	.menu-label {
		flex: 1;
		font-size: 16px;
		color: #333;
	}

	.menu-extra {
		font-size: 15px;
		color: #999;
		margin-right: 6px;
	}

	.right-arrow {
		width: 8px;
		height: 8px;
		border-top: 2px solid #C7C7CC;
		border-right: 2px solid #C7C7CC;
		transform: rotate(45deg);
	}

	.bottom-actions {
		display: flex;
		flex-direction: row;
		padding: 20px 16px;
	}

	.btn {
		flex: 1;
		height: 46px;
		border-radius: 6px;
		display: flex;
		align-items: center;
		justify-content: center;
		font-size: 16px;
		font-weight: 500;
	}

	.btn-primary {
		background-color: #07C160;
		color: #fff;
		margin-right: 12px;
	}

	.btn-outline {
		background-color: #fff;
		color: #07C160;
		border: 1px solid #07C160;
	}

	.btn-last {
		margin-right: 0;
	}
</style>

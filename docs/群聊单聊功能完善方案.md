# 群聊 / 单聊功能完善方案（对标微信）

> 版本：v2.0　日期：2026-08-20
> 关联文档：`docs/IM系统架构设计.md`、`docs/桌面端本地存储方案.md`、`docs/执行计划.md`
> 目标：以微信桌面端为参照，补齐群聊与单聊的核心交互功能，按优先级分期落地。
> **实现状态**：第一期（P0）已全部实现 ✅；第二期（P1）除 G9/S8 外已全部实现（G9/S8 已实现后按产品决策取消展示）；第三期（P2）已全部实现 ✅（含 0014_group_p2.sql 迁移）。详见文内各表「状态」列。

---

## 1. 现状盘点（已具备能力）

整理自现有代码，避免重复建设：

| 领域 | 已实现 | 位置 |
|---|---|---|
| 消息类型 | 文本/图片/文件/语音/视频/系统/合并转发（1~7） | `service/internal/message`、`MainWindow.vue` MSG_TYPE |
| 收发链路 | WS 实时推送 + ack 确认 + 超时重发 + HTTP 降级 + 离线发送队列 | `ws.js`、`gateway/` |
| 撤回 | 2 分钟内撤回、"撤回了一条消息"提示、撤回后重新编辑 | `POST /conversations/:id/recall` |
| 回执 | 送达 ack、已读 read（会话级已读游标，单聊双向同步） | `sendRead/sendAck` |
| 未读 | unread_count 列维护（撤回递减，不虚高）、托盘角标 | `0009_unread_count.sql` |
| 历史 | seq 分页 + 增量拉取（afterSeq）、本地 SQLite 秒开 | `localdb`、`repos.js` |
| 搜索 | 全局/单会话聊天记录搜索（本地 + 服务端） | `SearchChatHistoryModal.vue` |
| 群管理 | 建群、邀请、退群、群名/群公告（群主/管理员鉴权）、成员上限、入群/退群系统消息按身份渲染、移除成员、设/撤管理员、转让群主、群昵称、成员搜索 | `social/service.go` |
| 好友 | 申请/同意/拒绝、删除、备注（群聊内优先显示备注）、实时申请弹框（同意/拒绝/关闭） | `social/handler.go`、`FriendRequestPopup.vue` |
| 通话 | WebRTC 一对一语音通话（信令走 WS） | `api/call.js` |
| 通知 | 通知中心（好友申请/群邀请/系统/@提及）、WS 实时推送、群公告强提醒横幅 | `NotificationCenter.vue` |

**关键缺口**（与微信的差距，本文档要解决的）：

- 会话列表：置顶、免打扰、标记未读、草稿——`conversations` 表已有 `muted/pinned` 字段但无更新接口与 UI；
- 消息交互：引用回复、@提及、多选转发/逐条转发/批量删除、逐条转发外的合并转发；
- 群管理：移除成员、设管理员、转让群主、群昵称、群禁言、入群确认、群文件面板、保存到通讯录；
- 体验细节：消息表情回应、群公告推送提醒、图片查看器等部分已有，需补齐交互。

---

## 2. 功能总表（对标微信，含优先级）

> 优先级定义：**P0** 高频刚需，第一期必做；**P1** 重要体验，第二期；**P2** 锦上添花，按需排期。

### 2.1 会话列表（单聊/群聊通用）

| # | 功能 | 微信参照 | 优先级 | 状态 | 说明 |
|---|---|---|---|---|---|
| L1 | 会话置顶 | 右键 → 置顶，置顶区灰色底 | P0 | ✅ | `PUT /conversations/:id/settings` + 右键菜单 |
| L2 | 消息免打扰 | 右键 → 免打扰；免打扰会话不提醒、角标变灰点 | P0 | ✅ | 同 L1 接口 + 资料面板开关 + 灰点角标 |
| L3 | 标记未读 / 已读 | 右键 → 标记未读（红点手动挂起） | P1 | ✅ | 纯本地 `marked_unread`，收新消息/打开会话自动清除 |
| L4 | 删除会话 | 右键 → 删除（可选同时清空聊天记录） | P1 | ✅ | `DELETE /conversations/:id`（仅删视图保留消息，再收发自动重建）+ 本地清理 |
| L5 | 草稿显示 | 会话列表红色"[草稿]"前缀 | P0 | ✅ | 本地 `draft` 列 |
| L6 | 排序规则 | 置顶优先 → 时间倒序 | P0 | ✅ | 随 L1 |
| L7 | 免打扰角标样式 | 免打扰会话不显示数字角标，显示灰点 | P1 | ✅ | 随 L2；列表角标灰点已实现；托盘角标区分已实现（L7：非免打扰未读→数字角标，仅免打扰未读→灰点角标 `badge.set({count:0,dot:true})`） |

### 2.2 单聊功能

| # | 功能 | 微信参照 | 优先级 | 状态 | 说明 |
|---|---|---|---|---|---|
| S1 | 引用回复 | 右键 → 引用，气泡上方带引用块，点击跳转 | P0 | ✅ | `extra.quote` 协议、独立引用气泡、点击定位、撤回降级、备注名动态解析 |
| S2 | 转发（逐条 + 合并） | 右键 → 转发给…；合并转发为卡片 | P1 | ✅ | 逐条 + 合并（`type=7`，content 存 JSON 摘要，上限 40 条防超 4000 字） |
| S3 | 多选 | 右键 → 多选，勾选后批量转发/删除 | P1 | ✅ | 多选勾选框 + 底部操作条（逐条/合并转发、删除、取消） |
| S4 | 删除消息 | 右键 → 删除（仅删本地视角） | P1 | ✅ | 多选批量删除，本地库同步删 |
| S5 | 撤回后重新编辑 | 已有 | — | ✅ | 保持 |
| S6 | 表情回应（Reaction） | 消息上贴表情 | P2 | ✅ | 独立表 `message_reactions`（非 extra）；`POST /conversations/:id/messages/:mid/reactions` body `{emoji, add}`；历史/增量接口批量聚合进消息 DTO；WS `reaction` 帧增量推送；气泡 hover 快捷表情栏 + 已添加反应横排（点击自己的可取消） |
| S7 | 单聊"对方正在输入" | 输入状态提示 | P2 | ✅ | WS `typing` 帧（不落库、不进离线队列，仅单聊对端转发）；客户端 3s 节流发送 + 会话头部"对方正在输入…"闪烁提示 3s 自动清除 |
| S8 | 聊天文件/图片聚合面板 | 资料面板 → 图片与文件 | P1 | ⚠️ | 已实现（本地库聚合 tab 面板）后按产品决策取消展示，组件已删除 |

### 2.3 群聊功能

| # | 功能 | 微信参照 | 优先级 | 状态 | 说明 |
|---|---|---|---|---|---|
| G1 | @提及 | 输入 @ 弹出成员选择，高亮显示；通知中心"@我" | P0 | ✅ | 候选弹层/过滤/插入/高亮、穿透免打扰强提醒、`[有人@我]` 摘要、不能 @自己；通知中心 @提及分类已落地（服务端写 `notifications type=mention`） |
| G2 | @所有人 | 仅群主/管理员 | P1 | ✅ | `extra.mention_uids=["all"]` 特殊标记（500 人展开会超 extra 2KB）+ 服务端角色校验（仅群主/管理员）+ 通知展开为除发送者外全部成员；客户端 @候选顶部"所有人"选项 |
| G3 | 移除群成员 | 群主/管理员在群资料面板减号移除 | P0 | ✅ | `POST /members/:uid/kick` + 权限鉴权 + `group_kick` 系统消息 + `group.kicked` 客户端清理 |
| G4 | 设为/取消管理员 | 群主操作 | P0 | ✅ | `PUT /members/:uid/role` + `group.role_changed` + 标签/hover 操作 |
| G5 | 转让群主 | 群主 → 选择新群主 → 确认 | P1 | ✅ | `PUT /groups/:gid/owner` + 选择弹窗 + `group.owner_changed` + 系统消息 |
| G6 | 群昵称（我在本群的昵称） | 群设置 → 我在本群的昵称 | P1 | ✅ | `0012` 迁移 + `PUT /members/me/nickname` + 面板编辑；展示优先级 备注 > 群昵称 > 昵称 |
| G7 | 入群确认（邀请需群主/管理员同意） | 群管理 → 群聊邀请确认 | P2 | ✅ | `groups.invite_confirm`（0014 迁移）+ `PUT /groups/:gid/settings` 开关 + 邀请转通知（`notifications type=group_invite_confirm` + WS `group.invite_pending`）+ `POST /groups/:gid/invites/decide` 同意/拒绝（仅群主/管理员） |
| G8 | 群禁言 | 全员禁言 / 指定成员禁言 | P2 | ✅ | `groups.mute_all` + `group_members.muted_until`（0014 迁移）+ 发送守卫注入 `GroupMuteCheck`（全员禁言豁免群主/管理员；个人禁言不豁免）+ `PUT /groups/:gid/settings`（全员）+ `PUT /groups/:gid/members/:uid/mute`（个人）+ 禁言系统消息 + `group.muted` 事件 + 输入框禁用态 |
| G9 | 群文件面板 | 群资料 → 文件列表（上传人/时间/大小） | P1 | ⚠️ | 已实现（本地库聚合）后按产品决策取消展示，组件已删除 |
| G10 | 保存到通讯录 | 群设置开关，关闭后群不在通讯录展示 | P2 | ✅ | `group_members.saved`（0014 迁移）+ `PUT /groups/:gid/saved` + 群列表 DTO 回显 saved + 通讯录群列表过滤 |
| G11 | 群公告强提醒 | 发布公告时全员推送弹窗提醒 | P1 | ✅ | `group.announcement` 事件（公告实际变化才推）+ 客户端顶部横幅可关闭 |
| G12 | 群内搜索成员 | 群成员列表搜索框 | P1 | ✅ | 前端过滤，按展示名匹配 |
| G13 | 群聊免打扰/置顶 | 同 L1/L2 | P0 | ✅ | 同一套接口 |
| G14 | 群已读回执展示 | 企业微信风格：显示 N 人已读 | P2 | ✅ | `GET /conversations/:id/messages/:mid/read_count`（仅群主/管理员可查，企业微信策略）；聚合 `message_reads` 游标；群聊气泡下方"已读 N 人"（仅管理员视角） |

> 额外已落地（超出原方案表）：群成员展示顺序（群主 → 管理员 → 按入群时间，`0013` 迁移加 `ORDER BY join_time`）与一行两列布局；聊天头部群聊显示 `群名 (N)`、单聊显示对方名称（备注优先）；会话列表最后消息展示 `发送者: 内容` 前缀（备注 > 群昵称 > 昵称动态解析，`conversations.last_sender_uid/name` 列）。

### 2.4 输入区通用

| # | 功能 | 微信参照 | 优先级 | 状态 | 说明 |
|---|---|---|---|---|---|
| I1 | 表情面板 | 😀 按钮弹出 emoji 面板 | P0 | ✅ | `EmojiPanel.vue` |
| I2 | 截图粘贴 | Ctrl+V 粘贴剪贴板图片直接发送 | P1 | ✅ | paste 事件取图 + Electron 主进程 `clipboard:read-image` 兜底 |
| I3 | 拖拽发送文件/图片 | 拖入聊天窗口即发 | P1 | ✅ | `useStagedFiles` drop 事件 + 拖入提示遮罩 |
| I4 | 草稿自动保存 | 切换会话保留输入内容（同 L5） | P0 | ✅ | 同 L5 |
| I5 | Enter 发送 / Shift+Enter 换行 | 可在设置中切换 | P1 | ✅ | 设置持久化 `workchat:send-with-enter`；关闭后 Enter 换行、Ctrl+Enter 发送，主窗口实时读取 |

---

## 3. 第一期（P0）功能详细设计

> ✅ 本期内全部功能（L1/L2/L5/L6/S1/G1/G3/G4/I1）已于 2026-08-20 前实现落地，以下设计描述与最终实现一致（细节以代码为准）：
> - L1/L2/L6：`PUT /api/v1/conversations/:id/settings`；
> - S1：`extra.quote` 协议 + 引用块独立气泡 + 点击定位 + 撤回降级；
> - G1：`extra.mention_uids` + 客户端强提醒；服务端补充了通知中心落地（群消息落库时解析 `mention_uids` 为被 @ 成员写 `notifications type=mention`，消息服务经注入钩子调用）；
> - G3/G4：接口与文档一致；
> - I1：`EmojiPanel.vue`。

### 3.1 L1 会话置顶 + L6 排序

**交互**（对齐微信）：
- 会话项右键菜单：置顶聊天 / 取消置顶；置顶会话排成独立区块，背景浅灰；
- 排序：置顶区（内部按最后消息时间倒序）→ 普通区（按时间倒序）。

**服务端**：`conversations.pinned` 字段已存在，新增接口：

```
PUT /api/v1/conversations/:id/settings    body: { pinned?: 0|1, muted?: 0|1 }
```

- 只允许更新 owner_uid = 当前用户的会话行（鉴权防越权）；
- 返回更新后的会话 DTO；
- WS 侧不需要推送（设置是单人视角）。

**前端**：
- `api/message.js` 增加 `updateSettings(convId, {pinned, muted})`；
- 会话列表 computed 分两组渲染；右键菜单组件新增（ConversationContextMenu）；
- 本地 SQLite `conversations.pinned` 已有，写接口成功后更新本地。

### 3.2 L2 消息免打扰

**交互**：
- 右键菜单 → 消息免打扰（勾选态）；
- 免打扰会话：新消息不弹系统通知、不响铃、托盘角标不累加数字（P1 阶段改灰点，第一期先静默角标）；
- 群资料面板内也提供"消息免打扰"开关（微信在群设置里同样有）。

**实现**：同 L1 走 `PUT /conversations/:id/settings`；`ConversationDTO.muted` 已返回，前端通知逻辑（`notify.js` / `badge.js`）读取该标记跳过提醒。

### 3.3 L5/I4 草稿

**交互**：
- 切换会话时把输入框内容存为该会话草稿（纯本地，不同步服务端）；
- 会话列表最后消息位置显示红色 `[草稿] xxx`；
- 发出一条消息后清除该会话草稿。

**实现**：
- 本地 SQLite `conversations` 增加 `draft TEXT` 列（`db.js` 迁移）；
- MainWindow 切换会话 `watch(currentConvId)` 时保存/恢复；
- 草稿只在"最后一条消息时间之后未发送"场景展示，微信行为一致。

### 3.4 S1 引用回复

**交互**：
- 右键消息 → 引用；输入区上方出现引用条（发送者：内容摘要，× 可取消）；
- 发送后气泡上方带引用块（灰色小卡，显示被引用人 + 摘要）；点击引用块滚动定位到原消息；
- 被引用消息已删除/已撤回时显示"引用内容不存在"。

**协议设计**（不新增 type，走 extra，兼容现有渲染链路）：

```jsonc
// extra 增加 quote 字段
{
  "quote": {
    "msg_id": "1234567890",      // 被引用消息 id（字符串防精度丢失，与现有约定一致）
    "seq": 42,
    "sender_uid": "1001",
    "sender_name": "林晚",
    "type": 1,                    // 被引用消息类型
    "content": "原文摘要（截断 100 字）"
  }
}
```

- 服务端无需改动核心链路（extra 已透传，2KB 上限内）；仅校验 quote.msg_id 存在性可选（防伪造，P1 再收紧）；
- 本地库消息 extra 列已存 JSON，无需迁移；
- 点击定位：本地库按 msg_id 查 seq，若不在已加载区间则触发 beforeSeq 翻页加载。

### 3.5 G1 @提及

**交互**：
- 群聊输入框键入 `@` 弹出成员下拉（头像 + 群昵称/备注），支持继续输入过滤；
- 选中后插入 `@名字 `（名字用群内显示名），光标后移；@ 文本在气泡内高亮；
- 被 @ 的人：即使开了免打扰也强提醒（微信行为），通知中心"@提及"分类落地（该分类 UI 已有占位）；
- 群聊会话列表摘要显示 `[有人@我] 消息内容`（微信行为）。

**协议设计**：

```jsonc
// extra 增加 mention 字段
{ "mention_uids": ["1001", "1002"] }
```

- 发送时前端收集 @ 到的 uid 列表写入 extra；
- 服务端 `msgsToDTOs` 无需解析；推送帧原样携带 extra，接收方客户端判断 `mention_uids` 含自己 → 强提醒 + 摘要前缀；
- 被 @ 判断只在客户端做，服务端不做特殊分发（避免扩散写）。

### 3.6 G3 移除群成员

**服务端**：

```
DELETE /api/v1/groups/:gid/members/:uid
```

- 鉴权：操作者必须是群主（0）或管理员（1）；管理员不能移除群主或其他管理员（微信规则：管理员只能移除普通成员）；
- 不能移除自己（退群走已有 `DELETE /members/me`）；
- 移除后：
  1. `group_members` 删除该行；`member_count - 1`；
  2. 系统消息（kind=`group_kick`）推送全体剩余成员："你被 xx 移出群聊"（被移除者视角）/ "xx 将 yy 移出了群聊"（其他成员视角），沿用 `SendGroupSystemMessageTo` 的分身份渲染模式；
  3. 被移除者：会话标记不可再发送（发送守卫已有"非成员拒绝"），客户端收到 `group.kicked` 推送后会话置灰或移除。

**前端**：群资料面板成员网格增加"−"按钮（仅群主/管理员可见），点击弹确认框。

### 3.7 G4 设为/取消管理员

**服务端**：

```
PUT /api/v1/groups/:gid/members/:uid/role    body: { role: 1 | 2 }
```

- 仅群主可操作；role 取值 1（管理员）/ 2（普通成员），不能把管理员设为群主（转让走 G5）；
- 变更后向全体群成员推送 `group.role_changed`（客户端更新成员列表角标"管理员"标签，微信在成员头像旁有橙色"管理员"字样）；
- `group_members.role` 字段已有（AddGroupMember 第三参数），无需迁移。

**前端**：群成员列表项右键/悬浮菜单：设为管理员 / 取消管理员（仅群主可见）；管理员成员显示橙色标签。

### 3.8 I1 表情面板

- 输入区新增 😀 按钮，弹出 emoji 网格面板（分类：常用/笑脸/手势/动物/物品，纯 Unicode）；
- 点击插入到输入框光标处；面板外点击关闭；
- 纯前端实现，无服务端改动。

---

## 4. 第二期（P1）功能要点

| 功能 | 状态 | 设计要点与最终实现 |
|---|---|---|
| S2/S3 多选与转发 | ✅ | 多选模式：气泡左侧出现勾选框，底部操作条（逐条转发/合并转发/删除/取消）。逐条转发 = 逐条调用发送；合并转发 = 新消息类型 `type=7`，content 存 JSON `{count, items:[{sender_name, type, content}]}`，点击打开合并消息详情弹层。**与设计的差异**：条目上限 40、单条摘要 40 字（服务端 content 上限 4000 字约束，原设计 100 条会超限） |
| S4 删除消息 | ✅ | 多选批量删除，仅本地视角（内存 + 本地库同步删行） |
| G5 转让群主 | ✅ | `PUT /groups/:gid/owner`，系统消息 "xx 成为新群主"，`group.owner_changed` 事件客户端刷新角色 |
| G6 群昵称 | ✅ | `0012` 迁移 `nickname` 列；`PUT /groups/:gid/members/me/nickname`；展示优先级 备注 > 群昵称 > 昵称（群资料面板、成员搜索、@候选、消息发送者名、会话列表前缀一致应用） |
| G9 群文件面板 | ⚠️ | 曾以本地聚合（按会话查 type=3 消息）+ 图片/文件 tab 实现，**后按产品决策取消展示**，组件已删除 |
| G11 群公告强提醒 | ✅ | `group.announcement` 事件（服务端对比变更前公告，仅实际变化时推送，操作者自身不推）；客户端缓存待展示公告，打开对应群会话时顶部横幅展示、可关闭 |
| L3 标记未读 | ✅ | 本地 `marked_unread` 列（v6 迁移）；右键设置；无未读时显示灰点；收到新消息/打开会话自动清除。**注意**：该列 NOT NULL 且不参与 upsert（显式 NULL 会违反约束），状态变更走专用 `setMarkedUnread` |
| L4 删除会话 | ✅ | `DELETE /api/v1/conversations/:id`（归属校验，仅删视图行保留消息）；客户端本地列表/本地库/映射即时清理，服务端失败仅提示不回滚 |
| I2 截图粘贴 | ✅ | 输入框 paste 事件取图直接暂存发送；无图且无文本时兜底 Electron 主进程 `clipboard:read-image`（PNG dataURL） |
| I3 拖拽发送 | ✅ | 聊天区 drop 事件 → `useStagedFiles` 暂存（白名单/大小校验），拖入时提示遮罩 |
| S8 图片与文件面板 | ⚠️ | 曾实现（单聊/群聊资料面板 tab 聚合面板），**后按产品决策取消展示**，组件已删除 |

---

## 5. 第三期（P2）功能要点

> ✅ 本期内全部功能（G7/G2/G8/G10/G14/S6/S7/L7）已于 2026-08-20 全部实现落地（迁移 `0014_group_p2.sql`）。

| 功能 | 设计要点 | 最终实现差异 |
|---|---|---|
| G7 入群确认 | 群设置开关 `invite_confirm`；开启后成员邀请不直接入群，改为向群主/管理员发通知（复用 notifications type=invite），同意后走现有 InviteToGroup | 通知类型独立为 `group_invite_confirm`（避免与群邀请 invite 混淆），action 携带 g_uid/inviter/invitee 供通知中心直接同意/拒绝；新增 `POST /groups/:gid/invites/decide` |
| G8 群禁言 | `groups` 加 `mute_all`；`group_members` 加 `muted_until`；发送守卫检查；系统消息提示"群主开启了全员禁言" | `muted_until` 存 unix 毫秒 BIGINT（避免 DATETIME 时区问题）；守卫经 `GroupMuteCheck` 注入消息服务；全员禁言豁免群主/管理员、个人禁言不豁免（微信规则） |
| G10 保存到通讯录 | `group_members` 加 `saved`；通讯录群列表过滤 | 新增 `PUT /groups/:gid/saved`（任何成员操作自己的行）；群列表/群详情 DTO 回显 saved |
| G14 群已读人数 | 读已读游标表聚合 count(read_seq >= msg.seq)，仅群主/管理员可查（企业微信策略）；接口 `GET /conversations/:id/messages/:mid/read_count` | 按文档实现；客户端打开群聊时对最近 50 条消息节流并发查询，气泡下方"已读 N 人"仅管理员视角 |
| S6 表情回应 | `POST /conversations/:id/messages/:mid/reactions` body `{emoji}`；消息 extra 或独立表；WS 推送增量 | **独立表** `message_reactions`（消息不可变 + extra 2KB 上限）；body 增加 `{add}` 区分添加/移除；历史/增量接口批量聚合 reactions 进消息 DTO；WS `reaction` 帧 `{conv_id, msg_id, uid, emoji, add}` |
| S7 正在输入 | WS 新帧 `typing`（不落库、不转发历史），单聊对端会话头部显示"对方正在输入…" | 按文档实现：网关 `HandleTyping` 仅单聊转发对端（群聊不广播）；客户端 3s 节流发送 + 3s 无新帧自动清除 |
| L7 免打扰灰点角标 | 托盘角标区分：免打扰会话只贡献红点不贡献数字 | `badge.set({count, dot})`：非免打扰未读>0 → 数字角标；仅免打扰未读>0 → 灰点角标（`renderDotPNG`） |

---

## 6. 数据与协议变更汇总

### 6.1 服务端迁移（实际已落地的 migrations）

```sql
-- 0012_group_enhance.sql（第二期，已执行）
ALTER TABLE group_members ADD COLUMN nickname VARCHAR(40) DEFAULT NULL COMMENT '群内昵称';
-- 0013_conv_last_sender.sql（会话列表发送者前缀，已执行）
ALTER TABLE conversations ADD COLUMN last_sender_uid BIGINT DEFAULT NULL COMMENT '最后消息发送者 uid';
ALTER TABLE conversations ADD COLUMN last_sender_name VARCHAR(40) DEFAULT NULL COMMENT '最后消息发送者昵称快照';
```

> 第三期迁移 `0014_group_p2.sql`（已执行，2026-08-20）：
> 与原预留 SQL 的差异：`muted_until` 由 DATETIME 改为 BIGINT（unix 毫秒，避免时区/JSON 序列化问题），并新增表情回应表 `message_reactions`：

```sql
ALTER TABLE `groups` ADD COLUMN invite_confirm TINYINT NOT NULL DEFAULT 0 COMMENT '邀请需确认';
ALTER TABLE `groups` ADD COLUMN mute_all TINYINT NOT NULL DEFAULT 0 COMMENT '全员禁言';
ALTER TABLE group_members ADD COLUMN muted_until BIGINT DEFAULT NULL COMMENT '单人禁言截止(unix 毫秒)';
ALTER TABLE group_members ADD COLUMN saved TINYINT NOT NULL DEFAULT 1 COMMENT '保存到通讯录';
CREATE TABLE IF NOT EXISTS message_reactions (
  id BIGINT PRIMARY KEY, conv_id BIGINT NOT NULL, msg_id BIGINT NOT NULL,
  uid BIGINT NOT NULL, emoji VARCHAR(16) NOT NULL, created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY uk_reaction (msg_id, uid, emoji), KEY idx_reaction_conv_msg (conv_id, msg_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='消息表情回应（S6）';
```

第一期**无需**服务端迁移（pinned/muted/role 字段均已存在）。

### 6.2 客户端本地库迁移（实际已落地）

```sql
-- desktop_client/electron/store/db.js 已执行版本
-- v4：会话草稿
ALTER TABLE conversations ADD COLUMN draft TEXT;
-- v5：会话最后消息发送者
ALTER TABLE conversations ADD COLUMN last_sender_uid TEXT;
ALTER TABLE conversations ADD COLUMN last_sender_name TEXT;
-- v6：标记未读（NOT NULL，不参与 upsert，走专用 setMarkedUnread）
ALTER TABLE conversations ADD COLUMN marked_unread INTEGER NOT NULL DEFAULT 0;
```

### 6.3 extra 结构化字段约定（消息 extra JSON）

| 字段 | 类型 | 引入期 | 状态 | 说明 |
|---|---|---|---|---|
| quote | object | 一期 | ✅ | 引用块 `{msg_id, seq, sender_uid, sender_name, type, content}`；发送者名渲染时动态解析（备注 > 群昵称 > 快照） |
| mention_uids | string[] | 一期 | ✅ | 被 @ 的成员 uid；服务端落库后为被 @ 成员写通知中心条目；**G2：含 `"all"` 标记时服务端按角色校验并展开为全部成员** |
| merge | object | 二期 | ✅ | 合并转发——**实际实现为 content 字段存 JSON**（`{count, items}`），非 extra 字段 |

> 注意 extra 有 2KB 上限（`maxExtraBytes`），quote.content 与 merge.items 需截断；合并转发条目超过限制时 content 仅存摘要、完整列表另存（二期细化）。
> S6 表情回应**不存 extra**（消息不可变 + 2KB 上限），改存独立表 `message_reactions`，历史/增量接口批量聚合进消息 DTO。

### 6.4 新增 HTTP 接口汇总

| 方法 | 路径 | 期次 | 状态 | 说明 |
|---|---|---|---|---|
| PUT | /api/v1/conversations/:id/settings | 一 | ✅ | 置顶/免打扰开关 |
| POST | /api/v1/groups/:gid/members/:uid/kick | 一 | ✅ | 移除成员（原设计 DELETE /members/:uid 与 Gin 已有 /members/me 静态段路由冲突，改为 POST 子路径） |
| PUT | /api/v1/groups/:gid/members/:uid/role | 一 | ✅ | 设/撤管理员 |
| DELETE | /api/v1/conversations/:id | 二 | ✅ | 删除会话 |
| PUT | /api/v1/groups/:gid/owner | 二 | ✅ | 转让群主 |
| PUT | /api/v1/groups/:gid/members/me/nickname | 二 | ✅ | 群昵称 |
| PUT | /api/v1/groups/:gid/settings | 三 | ✅ | 入群确认（G7）/全员禁言（G8）开关，仅群主/管理员 |
| POST | /api/v1/groups/:gid/invites/decide | 三 | ✅ | 入群确认处理（G7）：body `{invitee_uid, accept}`，仅群主/管理员 |
| PUT | /api/v1/groups/:gid/members/:uid/mute | 三 | ✅ | 成员禁言（G8）：body `{until}` unix 毫秒，0 解除 |
| PUT | /api/v1/groups/:gid/saved | 三 | ✅ | 保存到通讯录（G10）：body `{saved}` |
| POST | /api/v1/conversations/:id/messages/:mid/reactions | 三 | ✅ | 表情回应（S6）：body `{emoji, add}` |
| GET | /api/v1/conversations/:id/messages/:mid/reactions | 三 | ✅ | 表情回应查询（S6） |
| GET | /api/v1/conversations/:id/messages/:mid/read_count | 三 | ✅ | 群消息已读人数（G14），仅群主/管理员 |

### 6.5 WS 新增事件帧（gateway 推送）

| 事件 | 期次 | 状态 | 载荷 |
|---|---|---|---|
| group.kicked | 一 | ✅ | `{g_uid, conv_id, operator_uid}` 被移除者会话失效 |
| group.role_changed | 一 | ✅ | `{g_uid, uid, role}` |
| group.announcement | 二 | ✅ | `{g_uid, announcement, operator_uid}` 强提醒横幅（公告实际变化才推，操作者不推） |
| group.invite_pending | 三 | ✅ | `{g_uid, group_name, inviter_uid, inviter_name, invitee_uids}` 入群确认待处理（G7） |
| group.muted | 三 | ✅ | `{g_uid, uid, until}` 成员禁言变更（G8，客户端刷新成员标签/输入禁用态） |
| reaction | 三 | ✅ | `{conv_id, msg_id, uid, emoji, add}` 表情回应增量（S6，单聊对端/群聊成员） |
| typing | 三 | ✅ | `{conv_id}` 正在输入（S7）：客户端→网关→对端转发，不落库、不进离线队列 |

> 额外已落地的事件载荷扩展：`friend.request` 事件增加 `req_id/nickname`（好友申请实时弹框用）。

---

## 7. 迭代排期与剩余工作

> ✅ 第一、二、三期全部完成；**无剩余待办**。

| 阶段 | 内容 | 状态 |
|---|---|---|
| 第一期 Sprint 1 | L1/L6 置顶 + L2 免打扰 + L5 草稿（右键菜单组件先行） | ✅ 完成 |
| 第一期 Sprint 2 | S1 引用回复 + G1 @提及 | ✅ 完成（含通知中心 @提及落地） |
| 第一期 Sprint 3 | G3 移除成员 + G4 管理员 + I1 表情面板 | ✅ 完成 |
| 第二期 | 转发/多选/删除、转让群主、群昵称、粘贴/拖拽发送、标记未读、删除会话、公告强提醒、Enter 设置 | ✅ 完成（G9/S8 面板实现后按产品决策取消展示） |
| 第三期 | 入群确认、禁言、@所有人、表情回应、正在输入、已读人数、保存到通讯录、托盘灰点 | ✅ 完成（2026-08-20，含 `0014_group_p2.sql` 迁移与 `test_p2_group.mjs` 冒烟脚本） |

**验收方式**：沿用现有 `scripts/test_*.mjs` 自动化脚本模式，第三期新增 `scripts/test_p2_group.mjs`（HTTP API 冒烟：G7/G8/G2/G10/S6/G14 核心链路）与 `scripts/test_p2_mute_ui.mjs`（CDP UI 冒烟：全员禁言时输入框/表情/图片/文件/语音/发送按钮置灰与恢复）。

---

## 8. 风险与注意点

1. **雪花 ID 精度**：quote.msg_id、mention_uids 一律字符串传输（沿用现有约定），防 JS Number 精度丢失；
2. **撤回与引用的联动**：被引用消息被撤回/删除时，引用块降级显示"引用内容不存在"，不做级联清理（写扩散成本高）；
3. **@提及与免打扰的优先级**：被 @ 时穿透免打扰提醒，这是微信明确行为，测试需覆盖；
4. **管理员权限边界**：管理员可移除普通成员、可改群名公告（已有），但不可动群主与其他管理员（G3 鉴权务必写清）；
5. **群昵称显示优先级**：备注 > 群昵称 > 用户昵称，涉及 `friendDisplayName` 逻辑调整（现有函数仅处理备注与昵称两级）；
6. **分表影响**：消息相关新字段（deleted）需同时迁移 messages_0~3 四张分表；
7. **向后兼容**：所有 extra 新字段均可选，旧客户端忽略未知字段即可，无需强制升级。
